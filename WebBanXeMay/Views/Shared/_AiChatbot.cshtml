<div id="aiChatbotWidget" class="ai-chatbot-widget">
    <button id="aiChatToggle" type="button" class="ai-chat-toggle">
        <span class="ai-chat-toggle-icon">💬</span>
        <span>Tư vấn & hỗ trợ</span>
    </button>

    <div id="aiChatPanel" class="ai-chat-panel d-none">
        <div class="ai-chat-header">
            <div class="ai-chat-header-left">
                <div class="ai-chat-title">Trợ lý tư vấn & hỗ trợ</div>
                <div class="ai-chat-subtitle">AI hỗ trợ trước, cần thiết có thể gặp nhân viên</div>
            </div>

            <div class="ai-chat-header-actions">
                <button id="aiChatNewConversation" type="button" class="ai-chat-action-btn">
                    + Chat mới
                </button>
                <button id="aiChatDeleteConversation" type="button" class="ai-chat-action-btn danger">
                    Xóa chat
                </button>
                <button id="aiChatClose" type="button" class="ai-chat-action-btn icon-btn">
                    ✕
                </button>
            </div>
        </div>

        <div class="ai-chat-body">
            <aside class="ai-chat-sidebar">
                <div class="ai-chat-sidebar-title">Lịch sử trò chuyện</div>
                <div id="aiChatConversationList" class="ai-chat-conversation-list"></div>
            </aside>

            <section class="ai-chat-main">
                <div id="aiChatEmptyState" class="ai-chat-empty-state">
                    <div class="ai-chat-empty-icon">🛵</div>
                    <div class="ai-chat-empty-title">Bắt đầu đoạn chat mới</div>
                    <div class="ai-chat-empty-text">
                        Bạn có thể hỏi về giá xe, tồn kho hoặc nhờ tư vấn mẫu xe phù hợp với nhu cầu.
                    </div>

                    <div class="ai-chat-suggest-list">
                        <button type="button" class="ai-suggest-btn" data-message="xe cho nữ khoảng 40 triệu">
                            Xe cho nữ khoảng 40 triệu
                        </button>
                        <button type="button" class="ai-suggest-btn" data-message="xe ga nào cho sinh viên">
                            Xe ga nào cho sinh viên
                        </button>
                        <button type="button" class="ai-suggest-btn" data-message="kiểm tra tồn kho Honda Vision">
                            Kiểm tra tồn kho Honda Vision
                        </button>
                    </div>
                </div>

                <div id="aiChatMessages" class="ai-chat-messages"></div>

                <div class="ai-chat-input-wrap">
                    <textarea id="aiChatInput"
                              class="ai-chat-input"
                              rows="2"
                              placeholder="Nhập câu hỏi của bạn..."></textarea>
                    <button id="aiChatSend" type="button" class="ai-chat-send-btn">
                        Gửi
                    </button>
                </div>
            </section>
        </div>
    </div>
</div>

<style>
    :root {
        --chat-bg: #111216;
        --chat-bg-2: #181a20;
        --chat-bg-3: #1f222a;
        --chat-bg-4: #262a34;
        --chat-border: #2d313c;
        --chat-border-soft: #383d49;
        --chat-text: #f3f4f6;
        --chat-muted: #a8afbb;
        --chat-pink: #e83e8c;
        --chat-pink-dark: #d63384;
        --chat-pink-soft: rgba(232, 62, 140, 0.16);
        --chat-red-soft: rgba(239, 68, 68, 0.15);
        --chat-shadow: 0 20px 45px rgba(0, 0, 0, 0.35);
        --chat-shadow-soft: 0 8px 22px rgba(0, 0, 0, 0.22);
        --chat-send: #ff6b35;
        --chat-send-dark: #ff5722;
    }

    .ai-chatbot-widget {
        position: fixed;
        right: 22px;
        bottom: 22px;
        z-index: 9999;
        font-family: "Segoe UI", Arial, sans-serif;
    }

    .ai-chat-toggle {
        border: none;
        background: linear-gradient(135deg, var(--chat-pink), var(--chat-pink-dark));
        color: #fff;
        padding: 13px 20px;
        border-radius: 999px;
        box-shadow: 0 14px 30px rgba(232, 62, 140, 0.28);
        cursor: pointer;
        font-weight: 700;
        font-size: 16px;
        display: flex;
        align-items: center;
        gap: 9px;
        transition: transform .2s ease, box-shadow .2s ease;
    }

    .ai-chat-toggle:hover {
        transform: translateY(-1px);
        box-shadow: 0 18px 34px rgba(232, 62, 140, 0.35);
    }

    .ai-chat-toggle-icon {
        font-size: 17px;
        line-height: 1;
    }

    .ai-chat-panel {
        width: 860px;
        max-width: calc(100vw - 28px);
        height: 610px;
        max-height: calc(100vh - 95px);
        background: var(--chat-bg);
        border-radius: 22px;
        box-shadow: var(--chat-shadow);
        overflow: hidden;
        margin-top: 12px;
        display: flex;
        flex-direction: column;
        border: 1px solid var(--chat-border);
    }

    .ai-chat-header {
        background: linear-gradient(135deg, #191b22, #14161b);
        color: var(--chat-text);
        padding: 15px 18px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 14px;
        border-bottom: 1px solid var(--chat-border);
    }

    .ai-chat-header-left {
        min-width: 0;
    }

    .ai-chat-title {
        color: #fff;
        font-size: 15px;
        font-weight: 800;
        margin-bottom: 3px;
    }

    .ai-chat-subtitle {
        color: var(--chat-muted);
        font-size: 12px;
    }

    .ai-chat-header-actions {
        display: flex;
        gap: 8px;
        align-items: center;
        flex-shrink: 0;
    }

    .ai-chat-action-btn {
        border: 1px solid var(--chat-border-soft);
        background: var(--chat-bg-3);
        color: #f3f4f6;
        padding: 9px 13px;
        border-radius: 12px;
        cursor: pointer;
        font-size: 13px;
        font-weight: 600;
        transition: all .2s ease;
    }

    .ai-chat-action-btn:hover {
        background: #2d313c;
        transform: translateY(-1px);
    }

    .ai-chat-action-btn.danger {
        background: rgba(220, 38, 38, 0.18);
        color: #fecaca;
        border-color: rgba(239, 68, 68, 0.25);
    }

    .ai-chat-action-btn.danger:hover {
        background: rgba(220, 38, 38, 0.26);
    }

    .ai-chat-action-btn.icon-btn {
        width: 40px;
        height: 40px;
        padding: 0;
        font-size: 16px;
    }

    .ai-chat-body {
        display: grid;
        grid-template-columns: 260px 1fr;
        min-height: 0;
        flex: 1;
        background: var(--chat-bg);
    }

    .ai-chat-sidebar {
        border-right: 1px solid var(--chat-border);
        background: var(--chat-bg-2);
        padding: 14px 12px;
        overflow-y: auto;
    }

    .ai-chat-sidebar-title {
        font-size: 14px;
        font-weight: 800;
        margin-bottom: 12px;
        color: var(--chat-text);
        padding: 2px 6px;
    }

    .ai-chat-conversation-list {
        display: flex;
        flex-direction: column;
        gap: 10px;
    }

    .ai-chat-conversation-item {
        text-align: left;
        border: 1px solid var(--chat-border-soft);
        background: var(--chat-bg-3);
        border-radius: 16px;
        padding: 12px;
        cursor: pointer;
        transition: all .2s ease;
        box-shadow: none;
    }

    .ai-chat-conversation-item:hover {
        border-color: var(--chat-pink);
        background: #2a2f39;
        transform: translateY(-1px);
    }

    .ai-chat-conversation-item.active {
        border-color: var(--chat-pink);
        background: linear-gradient(180deg, rgba(232, 62, 140, 0.14), rgba(232, 62, 140, 0.06));
        box-shadow: 0 6px 18px rgba(232, 62, 140, 0.12);
    }

    .ai-chat-conversation-title {
        font-weight: 800;
        font-size: 14px;
        color: #fff;
        margin-bottom: 6px;
        line-height: 1.35;
    }

    .ai-chat-conversation-preview {
        font-size: 12px;
        color: var(--chat-muted);
        line-height: 1.45;
        margin-bottom: 8px;
        display: -webkit-box;
        -webkit-line-clamp: 2;
        -webkit-box-orient: vertical;
        overflow: hidden;
        min-height: 34px;
    }

    .ai-chat-conversation-time {
        font-size: 11px;
        color: #8f97a5;
        font-weight: 600;
    }

    .ai-chat-no-history {
        color: var(--chat-muted);
        font-size: 13px;
        padding: 10px 6px;
    }

    .ai-chat-main {
        display: flex;
        flex-direction: column;
        min-width: 0;
        min-height: 0;
        background: #12141a;
        position: relative;
    }

    .ai-chat-empty-state {
        padding: 18px 20px 14px;
        border-bottom: 1px solid var(--chat-border);
        background: linear-gradient(180deg, #181b22, #14171d);
    }

    .ai-chat-empty-icon {
        width: 42px;
        height: 42px;
        border-radius: 50%;
        background: var(--chat-pink-soft);
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 20px;
        margin-bottom: 10px;
    }

    .ai-chat-empty-title {
        font-size: 18px;
        font-weight: 800;
        color: #fff;
        margin-bottom: 6px;
    }

    .ai-chat-empty-text {
        color: var(--chat-muted);
        font-size: 14px;
        margin-bottom: 12px;
        line-height: 1.5;
        max-width: 620px;
    }

    .ai-chat-suggest-list {
        display: flex;
        flex-wrap: wrap;
        gap: 8px;
    }

    .ai-suggest-btn {
        border: 1px solid var(--chat-border-soft);
        background: var(--chat-bg-3);
        color: #f2c4d8;
        border-radius: 999px;
        padding: 8px 12px;
        cursor: pointer;
        font-size: 13px;
        font-weight: 600;
        transition: all .2s ease;
    }

    .ai-suggest-btn:hover {
        background: var(--chat-pink-soft);
        border-color: var(--chat-pink);
        color: #fff;
    }

    .ai-chat-messages {
        flex: 1;
        overflow-y: auto;
        padding: 18px 18px 12px;
        background: #12141a;
    }

    .ai-chat-messages::-webkit-scrollbar,
    .ai-chat-sidebar::-webkit-scrollbar {
        width: 6px;
    }

    .ai-chat-messages::-webkit-scrollbar-thumb,
    .ai-chat-sidebar::-webkit-scrollbar-thumb {
        background: #3a3f4b;
        border-radius: 999px;
    }

    .ai-chat-messages::-webkit-scrollbar-thumb:hover,
    .ai-chat-sidebar::-webkit-scrollbar-thumb:hover {
        background: #4a5060;
    }

    .ai-msg {
        max-width: 74%;
        margin-bottom: 10px;
        padding: 14px 16px;
        border-radius: 18px;
        line-height: 1.65;
        font-size: 14px;
        word-break: break-word;
        animation: aiFadeIn .18s ease;
    }

    @@keyframes aiFadeIn {
        from {
            opacity: 0;
            transform: translateY(6px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .ai-msg.user {
        margin-left: auto;
        background: linear-gradient(135deg, var(--chat-pink), var(--chat-pink-dark));
        color: #fff;
        border-bottom-right-radius: 6px;
        box-shadow: 0 10px 24px rgba(232, 62, 140, 0.22);
    }

    .ai-msg.bot {
        margin-right: auto;
        background: var(--chat-bg-3);
        color: var(--chat-text);
        border: 1px solid var(--chat-border-soft);
        border-bottom-left-radius: 6px;
        box-shadow: var(--chat-shadow-soft);
    }

    .ai-msg-welcome {
        background: var(--chat-bg-3);
        border: 1px solid var(--chat-border-soft);
        box-shadow: none;
    }

    .ai-msg-text {
        white-space: normal;
    }

    .ai-msg-text p {
        margin: 0 0 8px;
    }

    .ai-msg-text p:last-child {
        margin-bottom: 0;
    }

    .ai-msg-text ul,
    .ai-msg-text ol {
        margin: 6px 0 8px 18px;
        padding: 0;
    }

    .ai-msg-text li {
        margin: 3px 0;
    }

    .ai-msg-text strong {
        color: #ffffff;
        font-weight: 800;
    }

    .ai-msg-text em {
        color: #d9dee7;
    }

    .ai-msg-text code {
        background: #0f1117;
        border: 1px solid var(--chat-border-soft);
        border-radius: 6px;
        padding: 1px 6px;
        font-family: Consolas, "Courier New", monospace;
        font-size: 12px;
        color: #fbbf24;
    }

    .ai-msg-text pre {
        margin: 8px 0;
        background: #0f1117;
        border: 1px solid var(--chat-border-soft);
        border-radius: 10px;
        padding: 10px 12px;
        overflow-x: auto;
    }

    .ai-msg-text pre code {
        border: none;
        background: transparent;
        padding: 0;
        color: #e5e7eb;
        font-size: 12px;
    }

    .ai-msg-text blockquote {
        margin: 8px 0;
        padding: 6px 10px;
        border-left: 3px solid #f9a8d4;
        background: rgba(249, 168, 212, 0.08);
        border-radius: 0 8px 8px 0;
        color: #e5e7eb;
    }

    .ai-msg-text a {
        color: #f9a8d4;
        text-decoration: underline;
    }

    .ai-product-card {
        margin-top: 10px;
    }

    .ai-product-image {
        max-width: 220px;
        border-radius: 14px;
        display: block;
        border: 1px solid var(--chat-border-soft);
    }

    .typing .ai-msg-text {
        color: #c2c8d1;
        font-style: italic;
    }

    .ai-chat-input-wrap {
        border-top: 1px solid var(--chat-border);
        padding: 14px;
        display: flex;
        gap: 10px;
        align-items: flex-end;
        background: #151820;
    }

    .ai-chat-input {
        flex: 1;
        resize: none;
        border: 1px solid var(--chat-border-soft);
        border-radius: 18px;
        padding: 12px 14px;
        outline: none;
        min-height: 52px;
        font-size: 14px;
        color: var(--chat-text);
        background: var(--chat-bg-3);
        transition: border-color .2s ease, box-shadow .2s ease;
    }

    .ai-chat-input::placeholder {
        color: #8f97a5;
    }

    .ai-chat-input:focus {
        border-color: var(--chat-pink);
        box-shadow: 0 0 0 4px rgba(232, 62, 140, 0.12);
    }

    .ai-chat-send-btn {
        border: none;
        background: linear-gradient(135deg, var(--chat-send), var(--chat-send-dark));
        color: #fff;
        padding: 0 20px;
        height: 52px;
        border-radius: 16px;
        cursor: pointer;
        font-weight: 800;
        font-size: 15px;
        transition: transform .2s ease, box-shadow .2s ease, opacity .2s ease;
        box-shadow: 0 10px 22px rgba(255, 106, 53, 0.24);
    }

    .ai-chat-send-btn:hover {
        transform: translateY(-1px);
    }

    .ai-chat-send-btn.disabled {
        opacity: .65;
        cursor: not-allowed;
        transform: none;
    }

    .d-none {
        display: none !important;
    }

    @@media (max-width: 992px) {
        .ai-chat-panel {
            width: calc(100vw - 18px);
            height: calc(100vh - 90px);
            right: 0;
        }

        .ai-chat-body {
            grid-template-columns: 1fr;
        }

        .ai-chat-sidebar {
            max-height: 180px;
            border-right: none;
            border-bottom: 1px solid var(--chat-border);
        }

        .ai-msg {
            max-width: 88%;
        }
    }

    @@media (max-width: 576px) {
        .ai-chatbot-widget {
            right: 10px;
            bottom: 10px;
        }

        .ai-chat-toggle {
            padding: 12px 18px;
            font-size: 15px;
        }

        .ai-chat-header {
            padding: 14px;
        }

        .ai-chat-header-actions {
            gap: 6px;
        }

        .ai-chat-action-btn {
            padding: 8px 10px;
            font-size: 12px;
        }

        .ai-chat-title {
            font-size: 14px;
        }

        .ai-chat-subtitle {
            font-size: 11px;
        }
    }

    .ai-msg-human-suggest {
        margin-top: -8px !important;
        padding-top: 0 !important;
    }

        .ai-msg-human-suggest.ai-msg.bot {
            background: transparent;
            border: none;
            box-shadow: none;
            padding: 0 16px 10px;
        }

    .ai-human-support-card {
        margin-top: 0;
        padding: 10px 12px;
        border-radius: 14px;
        background: rgba(34, 197, 94, 0.10);
        border: 1px solid rgba(34, 197, 94, 0.28);
        max-width: 100%;
    }

    .ai-human-support-title {
        font-size: 13px;
        font-weight: 800;
        color: #d1fae5;
        margin-bottom: 3px;
    }

    .ai-human-support-text {
        color: #e5e7eb;
        font-size: 13px;
        line-height: 1.45;
        margin-bottom: 8px;
    }

    .ai-human-support-btn {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        border: none;
        border-radius: 999px;
        padding: 7px 13px;
        background: linear-gradient(135deg, #22c55e, #16a34a);
        color: #fff !important;
        font-size: 13px;
        font-weight: 800;
        cursor: pointer;
        box-shadow: 0 8px 18px rgba(22, 163, 74, 0.22);
        transition: transform .2s ease, filter .2s ease;
    }

        .ai-human-support-btn:hover {
            transform: translateY(-1px);
            filter: brightness(1.05);
        }
</style>